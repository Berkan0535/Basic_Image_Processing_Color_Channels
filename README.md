📘 README.md

````markdown
# 🎨 Temel Görüntü İşleme ve Renk Kanalları

Bu Jupyter Notebook, dijital görüntü işleme alanında **renk kanalları**, **piksel manipülasyonu** ve **temel görüntü oluşturma tekniklerini** öğretmeyi amaçlamaktadır. Uygulamalar, Python'un `numpy` ve `matplotlib` kütüphaneleri kullanılarak gerçekleştirilmiştir.

---

## 🧠 İçerik Başlıkları

- **RGB Temelleri:** Görüntülerin 3 kanallı (Red, Green, Blue) yapısını anlama  
- **Görüntü Oluşturma:** `numpy.zeros()` ve `numpy.random.randint()` ile renkli görüntüler üretme  
- **Renk Kanallarına Erişim:** Her kanalın ayrı ayrı manipülasyonu  
- **Piksel Düzeyinde İşlem:** Görüntüdeki belirli bölgelerin rengini değiştirme  
- **Maskeleme ile Renk Dönüşümü:** Belirli renkleri başka renklere dönüştürme  
- **Uygulama Örnekleri:**  
  - Almanya bayrağı oluşturma  
  - Fenerbahçe bayrağı oluşturma  
  - Renk değiştirme (örneğin mavi → kırmızı)

---

## 🧰 Kullanılan Kütüphaneler

```python
import numpy as np
import matplotlib.pyplot as plt
````

---

## 🚀 Çalıştırma

1. Gerekli kütüphaneleri yükleyin:

   ```bash
   pip install numpy matplotlib
   ```
2. Notebook’u çalıştırın:

   ```bash
   jupyter notebook Temel_Goruntu_Isleme_Renk_Kanallari.ipynb
   ```

---

## 📸 Örnek Çıktılar

* Kırmızı, Yeşil, Mavi görüntülerin ayrı ayrı gösterimi
* Bayrak örnekleri (Almanya, Fenerbahçe)
* Renk dönüşümü: Belirli piksel renklerinin değiştirilmesi

---

## 📚 Eğitim Amacı

Bu çalışma, **dijital görüntü işlemenin temellerini** anlamak için hazırlanmıştır.
Öğrenciler, bu dosya aracılığıyla:

* Piksel yapısını kavrayabilir,
* Renk kanalları üzerinde işlem yapmayı öğrenebilir,
* Görüntü manipülasyonu için `NumPy` matrislerini kullanmayı deneyimleyebilir.

---
